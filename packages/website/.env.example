# =============================================================================
# Website Environment Variables
# =============================================================================
# For local development, copy this file to .env and update values as needed.
# For Railway deployment, see RAILWAY_DEPLOYMENT.md for configuration details.
# =============================================================================

# -----------------------------------------------------------------------------
# BUILD-TIME VARIABLES (VITE_*)
# -----------------------------------------------------------------------------
# These variables are injected during the build process and bundled into the
# client-side JavaScript. Changing them requires a rebuild of the application.
#
# For Railway: Set these in the service's Variables tab. After changing,
# trigger a redeploy to rebuild with the new values.
# -----------------------------------------------------------------------------

# WebSocket server URL for game client connection
# - Local development: ws://localhost:3001
# - Railway production: wss://your-game-server.railway.app (use wss:// for secure WebSocket)
VITE_WSS_URL=ws://localhost:3001

# REST API base URL (optional)
# - Local development: http://localhost:3001
# - Railway production: https://your-game-server.railway.app
# VITE_API_BASE_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# RUNTIME VARIABLES
# -----------------------------------------------------------------------------
# These variables are read at server startup and can be changed without rebuild.
# For Railway: Set these in the service's Variables tab.
# -----------------------------------------------------------------------------

# Environment mode (development/production)
# NODE_ENV=development

# Server port (Railway sets this automatically, no need to configure)
# PORT=3000